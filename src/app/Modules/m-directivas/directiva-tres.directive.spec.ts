import { DirectivaTresDirective } from './directiva-tres.directive';

describe('DirectivaTresDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaTresDirective();
    expect(directive).toBeTruthy();
  });
});
