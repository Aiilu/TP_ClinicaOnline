import { DirectivaDosDirective } from './directiva-dos.directive';

describe('DirectivaDosDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaDosDirective();
    expect(directive).toBeTruthy();
  });
});
