import { DirectivaUnoDirective } from './directiva-uno.directive';

describe('DirectivaUnoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaUnoDirective();
    expect(directive).toBeTruthy();
  });
});
